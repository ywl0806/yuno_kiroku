version: '2'
sql:
  - schema: 'schema.sql'
    queries: 'queries'
    engine: 'postgresql'
    gen:
      go:
        package: 'db'
        out: '../internal/db'
        overrides:
          - db_type: "uuid"
            go_type: "string"
